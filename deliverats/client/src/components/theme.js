import { createTheme } from "@mui/material/styles";

export const greyTheme = createTheme({
  palette: {
    primary: {
      main: "#C0C0C2",
    },
    secondary: {
      main: "#fafafa",
    },
  },
});
