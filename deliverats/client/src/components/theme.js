import { createTheme } from "@mui/material/styles";

export const greyTheme = createTheme({
  palette: {
    primary: {
      main: "#EEEEEE",
    },
    secondary: {
      main: "#fafafa",
    },
  },
});
